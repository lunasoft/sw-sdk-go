# SW SDK Go - Variables de Entorno
# Copia este archivo como .env y configura tus valores

# URLs de los servicios
SW_BASE_URL=https://services.test.sw.com.mx
SW_API_BASE_URL=https://api.test.sw.com.mx

# Credenciales de autenticación (opcional si usas token infinito)
SW_USER=tu-usuario@sw.com.mx
SW_PASSWORD=tu-password

# Token infinito (recomendado para producción)
SW_TOKEN=tu-token-infinito-aqui

# Notas:
# - Si SW_TOKEN está configurado, se usará automáticamente
# - Si SW_TOKEN está vacío, se usará autenticación tradicional (SW_USER + SW_PASSWORD)
# - Las URLs por defecto apuntan al entorno de pruebas
# - Para producción, cambia las URLs a los endpoints de producción
